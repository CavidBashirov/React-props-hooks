import React from 'react'
import '../../assets/css/slider.css'

function Slider(props) {
  return (
    <div>
        <section id='slider'>
          <h1>{props.name}</h1>
        </section>
    </div>
  )
}

export default Slider
